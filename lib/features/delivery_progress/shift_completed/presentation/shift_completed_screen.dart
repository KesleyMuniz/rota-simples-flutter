import 'package:flutter/material.dart';

class ShiftCompletedScreen extends StatelessWidget {
  const ShiftCompletedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ShiftCompletedScreen')),
      body: const Center(child: Text('ShiftCompletedScreen — em construção')),
    );
  }
}
