import 'package:flutter/material.dart';

class AddressReviewScreen extends StatelessWidget {
  const AddressReviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AddressReviewScreen')),
      body: const Center(child: Text('AddressReviewScreen — em construção')),
    );
  }
}
