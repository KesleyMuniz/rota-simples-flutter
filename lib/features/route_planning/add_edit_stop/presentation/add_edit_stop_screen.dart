import 'package:flutter/material.dart';

class AddEditStopScreen extends StatelessWidget {
  const AddEditStopScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AddEditStopScreen')),
      body: const Center(child: Text('AddEditStopScreen — em construção')),
    );
  }
}
