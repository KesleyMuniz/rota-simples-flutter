import 'package:flutter/material.dart';

class OptimizedRouteScreen extends StatelessWidget {
  const OptimizedRouteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('OptimizedRouteScreen')),
      body: const Center(child: Text('OptimizedRouteScreen — em construção')),
    );
  }
}
